spring:
  datasource:
    url: jdbc:mysql://localhost:3306/layout?characterEncoding=utf8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 初始化，最小，最大连接数
    initialSize: 1
    minidle: 1
    maxActive: 5